import React from "react";
import ReactDOM from "react-dom/client";
import WorkoutLogger from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <WorkoutLogger />
  </React.StrictMode>
);
